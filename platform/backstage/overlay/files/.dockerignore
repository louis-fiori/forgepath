.git
.yarn/cache
.yarn/install-state.gz
node_modules
packages/*/src
packages/*/node_modules
plugins
*.local.yaml
# Override: app-config.local.yaml is part of the forgepath overlay and
# *must* ship in the image, the deployment loads it via --config.
!app-config.local.yaml
