apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: forgepath-platform
  description: |
    The ForgePath platform itself, its architecture, the deploy/destroy flow, and the operations runbook are documented under the Docs tab.
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: documentation
  lifecycle: experimental
  owner: group:default/platform-team
  system: forgepath
