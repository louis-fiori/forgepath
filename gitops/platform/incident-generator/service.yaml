apiVersion: v1
kind: Service
metadata:
  name: incident-generator
  namespace: incident-generator
  labels:
    app: incident-generator
    backstage.io/kubernetes-id: incident-generator
spec:
  type: NodePort
  selector:
    app: incident-generator
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30081
